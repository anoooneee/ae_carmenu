Config = {}

Config.Opening = {
    Command = 'carmenu', 
    Keybind = 'F9'
}

Config.Access = {
    DriverOnly = false -- Acces menu only if driver
}

Config.Animations = {
    LockToggle = true -- soon
}

-- 'ox', 'anone', 'esx', 'okok' or 'custom'= just change in client (btw, anone will be soon)
Config.NotifySystem = 'ox'

Config.Locale = 'en'
